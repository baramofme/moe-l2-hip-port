# 未完成任务清单（2026-08-01 记录）

> 来源：moe-l2 产品化主线 + 副业内容线 + GitHub 维护
> 更新记录：2026-08-01 GitHub 同步（commit 2492b80 已推 public）后盘点

---

## 一、moe-l2 产品化主线（卖项目给 AI PC 厂商目标）

### P0（已规划，未开始）
- [ ] 可演示产品：整合 C++ patch + Python 调度层 → 一键安装包
- [ ] 产出完整 Benchmark 数据
- [ ] 写白皮书

### P1（已规划，未开始）
- [ ] 给 ollama / llama.cpp 提 issue / PR
- [ ] AI PC 厂商 PoC + Benchmark 包

### P2（已规划，部分有基础）
- [ ] 轻量分类器替换（Phase 3 剩余：LLM cache 集成、轻量分类器、GGUF 映射嵌入）
- [ ] GGUF metadata 内嵌映射
- [ ] 领域切换平滑过渡（**LRU 之上的策略层，非重写缓存**）
  - LRU 天然平滑：淘汰=最久未用先走，不做主动清空时旧领域专家自然衰减，新领域按需进入
  - 做法：① 不主动 clear（代码里禁掉"切换领域→cache.clear()"）② 共享专家靠 LRU 命中率自然保留 ③ 预热=检测到路由漂移→手动 insert 目标领域专家抬高 LRU 优先级
  - 依赖：模式 B/门控在线自适应提供"路由漂移"信号触发预热；共享专家清单从 collect 数据统计
  - 实施位置：排在门控在线自适应之后；预计几十行策略代码，A3 LRU cache 已提供底层机制
- [ ] LRU expert 缓存（Phase 3 GPU LRU cache 已实测，边界已确认）
- [ ] **门控在线自适应 + 模式 B（proxy 边用边收集）**：推理中读 LLAMA_EXPERT_LOG 实时路由，动态调缓存优先级（LRU 智能增强）；按会话累积路由画像
  - **数据飞轮**：模式 B 攒的真实流量（真实 prompt + 真实专家激活）→ 增量投喂轻量分类器训练集（冷启动用 collect 的 8 域种子数据，运行期用模式 B 真实数据）
  - 门控信息与分类器互补：分类器管冷启动（prompt→领域），门控管热循环（推理中→换专家）
  - **实施顺序**：① 先做轻量分类器骨架（用 collect 种子数据训第一版）→ ② 再上模式 B 增量重训（不然光攒数据没有消费方，飞轮转不起来）

### 方案 G（卡点）
- [ ] deepseek2.cpp 逐层调度改造：llm_graph_params 加 layer_single 参数、deepseek2.cpp 只构建单层、process_ubatch 逐层循环
  - 预期：RSS 23.3GB → ~600MB
  - **卡点**：llm_graph_context 类定义未找到
  - 现状：云机部署方案 G 代码（ggml-cuda.cu = 822dec，基于方案 F + A3 per-expert dispatch）

### 旧项（等用户明确要求才恢复）
- [ ] Mixtral collect（收集 Mixtral 路由数据）
- [ ] L0a Top-K pin
- [ ] 领域 TopK-pin C++ 实现
- [ ] git commit + push + PyPI 重发（如需新版本）

---

## 二、文档待办

- [x] 修订 `历史记录文档/Mixtral-速度显存测试-20260802.md`：补根因结论（--no-mmap 下 cache 无价值、慢速管线 3.1ms 元凶）→ git commit（cc5c5b7，2026-08-02）
- [x] 根因后续之文档修订：已完成（cc5c5b7，2026-08-02）
- [x] 根因后续之三档 DMA 全矩阵（0.5/1.0 更大容量）：不跑，此形态 cache 无用已盖棺
- [x] 修 a3_on 逻辑（2026-08-02 完成）：A3 管线只在专家真在 CPU（mmap 形态）时启用（cudaPointerGetAttributes 判断 src0->data 驻留位置，带缓存）；--no-mmap（专家全驻 GPU）形态保持快路径。实测 --no-mmap+cache：mmid 3.1ms→17µs、Gen 3.4→3.6 t/s；mmap 形态 A3 不变。备份：云机 /root/moe-l2-backups/a3on-fix-20260802/、本地 测试数据备份/a3on-fix-20260802/

---

## 三、副业内容线

- [ ] 副业第二篇：《出门也能管家里设备，导航页加个反代理就搞定》
  - 内容：nginx 反代 + WebSocket + base_url + 内外网联通
  - 注意：对新手信息量大，建议砍内容或加常见坑段落（已给建议，等用户决定）

---

## 四、GitHub 维护

- [ ] 用户有很多 GitHub 项目未更新，承诺之后更新（待用户指定哪些）
- [ ] README 完善 + PyPI token（v0.3.0 已发布，如再发新版本需 token）

---

## 已完成（本轮，无需再动）

- ✅ host buffer 专家 GPU 直算（2026-08-02，重大突破）：llama-model-loader 放开 mmap→host buffer 回退 + cli.py 加 GGML_OP_OFFLOAD_MIN_BATCH=1。实测 DS 12.5→37.5、Qwen 10→46.8 t/s，VRAM 1625/2147 MiB（专家不占显存）。bundle .so 已替换。备份：云机/本地 a3on-fix-20260802/
- ✅ 修 a3_on（2026-08-02）：--no-mmap+cache 的 mmid 3.1ms→17µs、Gen 3.4→3.6 t/s
- ✅ PyPI 0.4.0 重发（2026-08-02）：collect 新功能，wheel + sdist 上传成功；setuptools 69.5.1→>=77 修 metadata 2.1/2.4 License-File 兼容问题（commit e432e39）
- ✅ GitHub 全面同步（2026-08-01）：references 正式修复版 + README 根因结论 + collect 命令，commit 2492b80 推 public main
- ✅ 正式修复重放包 /opt/data/moe-l2-backups/official-fixes-20260802/
- ✅ 全量备份本地化 + 文件清单.md（md5 11 文件全核对）
